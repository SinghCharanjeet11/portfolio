# Email Service Configuration
# Choose one: sendgrid, mailgun, ses, nodemailer, mock
EMAIL_SERVICE=mock

# Email API Key (required for sendgrid, mailgun, ses)
EMAIL_API_KEY=your_api_key_here

# Email Addresses
FROM_EMAIL=noreply@yourdomain.com
TO_EMAIL=hello@yourdomain.com
FROM_NAME=Your Portfolio Contact Form
OWNER_NAME=Your Name

# SMTP Configuration (for nodemailer)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password

# AWS SES Configuration (for ses)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# Mailgun Configuration (for mailgun)
# MAILGUN_DOMAIN=yourdomain.com

# Next.js Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000
